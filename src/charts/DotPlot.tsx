import * as React from "react";

import { DotPlotSingle } from "./DotPlotSingle";

// TODO
export const DotPlot: React.StatelessComponent<{}> = (p) => {
  let grid: JSX.Element[] = [];
  // for (let i = 0; i < range[1]; i += skipTime) {
  //   grid.push(<line x1={x(i)} y1="0"
  //                   x2={x(i)} y2={visHeight}
  //                   stroke="lightgray" />);
  // }
  // grid.push(<line x1={0} y1={visHeight / 2} x2={visWidth} y2={visHeight / 2}
  // stroke="lightgray" />);
  return <></>;
}